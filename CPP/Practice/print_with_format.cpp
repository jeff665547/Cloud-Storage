#include <iostream>
int main(){

    int num;

    scanf("%d", &num); // 鍵盤輸入的整數存到num
    printf("%d \n", num); // 將整數num由螢幕輸出

    // printf("%c是文字，%d是整數，%f是小數。\n", 'A', 123, 10.5);
    
    // Tips:
    // print with format:
    // 1. print format, e.g.: %c, %f, %d, ...
    // 2. escape characters, e.g.: \n, \t, \\, ...
    // 3. decorators, e.g.: +, -, 9, ., ...

    

    return 0;
}